[build]
# Builds use the repo-root Dockerfile (single server npm ci). No Railpack buildCommand.

[deploy]
# No shell — Railway execs this directly, so avoid "cd ... &&" (cd is not an executable).
startCommand = "node /app/server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
