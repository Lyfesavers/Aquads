{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["cd server && npm ci --legacy-peer-deps"]
    }
  },
  "deploy": {
    "aptPackages": ["...", "fontconfig", "fonts-dejavu-core"]
  }
}
