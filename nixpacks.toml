[phases.setup]
nixPkgs = ["nodejs_18", "npm_9"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["SKIP_PREFLIGHT_CHECK=true CI=false npm run build"]

[start]
cmd = "serve -s build" 