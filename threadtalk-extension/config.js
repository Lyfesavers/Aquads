// ThreadTalk - shared config
// Production API host (Aquads Railway). For local development, switch to
// http://localhost:5000 and reload the extension.
// Keep in sync with manifest.json content_security_policy.connect-src.
var THREADTALK_API_ORIGIN = 'https://aquads-production.up.railway.app';
