/**
 * PWA install state — single listener lives in PWAInstallProvider (see App.js).
 */
export { usePWAInstall, PWAInstallProvider } from '../contexts/PWAInstallContext';
export { usePWAInstall as default } from '../contexts/PWAInstallContext';
