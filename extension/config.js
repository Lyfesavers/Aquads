// Keep in sync with extension/manifest.json "version".
var EXTENSION_VERSION = '1.4.8';

// Production API host (Railway). Keep in sync with manifest CSP connect-src.
var AQUADS_API_ORIGIN = 'https://aquads-production.up.railway.app';

/** Minimum From-side notional (USD) for swap affiliate points — matches aquads.xyz AquaSwap */
var MIN_SWAP_POINTS_USD = 5;
